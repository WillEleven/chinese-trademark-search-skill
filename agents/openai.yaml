version: "1.0.0"

interface:
  display_name: "中国商标查询 Skill"
  short_description: "连接托管平台 API，支持注册绑定、商标查询、导出与点数能力。"
  brand_color: "#1D4ED8"
  default_prompt: "Use $chinese_trademark_search_skill to guide users to register via tm.zhengquai.com, then check points, binding, and search or export Chinese trademark data. Always remind users that search results are informational only, not legal advice."

policy:
  allow_implicit_invocation: false
